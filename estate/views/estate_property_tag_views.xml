<?xml version="1.0"?>
<odoo>
    <!-- Vista di default -->
    <record id="estate_property_tag_action" model="ir.actions.act_window">
        <field name="name">Property Tags</field>
        <field name="res_model">estate.property.tag</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!-- Viste custom -->

    <!-- Vista List (Tree) -->
    <record id="estate_property_tag_tree_action" model="ir.ui.view">
        <field name="name">estate.property.tag.tree</field>
        <field name="model">estate.property.tag</field>
        <field name="arch" type="xml">
            <tree string="Estate Property Tags Tree">
                <field name="name"/>
            </tree>
        </field>
    </record>

    <!-- Vista Form (Form) -->
    <record id="estate_property_tag_form_action" model="ir.ui.view">
        <field name="name">estate.property.tag.form</field>
        <field name="model">estate.property.tag</field>
        <field name="arch" type="xml">
            <form string="Estate Property Tags Form">
                <sheet>
                    <group>
                        <field name="name"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Vista Search (Search) -->
    <record id="estate_property_tag_search_action" model="ir.ui.view">
        <field name="name">estate.property.tag.search</field>
        <field name="model">estate.property.tag</field>
        <field name="arch" type="xml">
            <search string="Estate Property Tags Serach">
                <field name="name"/>
            </search>
        </field>
    </record>
</odoo>